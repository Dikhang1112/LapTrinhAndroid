<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/blueduc" /> <!-- Màu nền -->
    <stroke
        android:width="2dp"
        android:color="@color/black" /> <!-- Màu viền -->
</shape>
