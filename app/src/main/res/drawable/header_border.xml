<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <stroke
        android:width="1dp"
        android:color="@color/black" /> <!-- Màu của đường viền -->

    <!-- Góc bo tròn (nếu muốn) -->
    <corners android:radius="10dp" />

    <!-- Background bên trong -->
    <solid android:color="@android:color/white" /> <!-- Màu nền -->

    <!-- Padding bên trong (nếu cần) -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>